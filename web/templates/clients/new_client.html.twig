{% extends 'base.html.twig' %}

{% block title %}Client list{% endblock %}

{% block body %}
    <div class="container">
        <div class="mt-5">
            {{ form_start(form, {'method': 'POST'}) }}
            {{ form_errors(form) }}
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}