{% extends 'base.html.twig' %}

{% block title %}Client list{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <h3 class="mx-auto my-3">New report information</h3>
        </div>
        <div class="mt-3 mx-3">
            <table class="table table-lg table-striped table-bordered">
                <thead>
                <tr>
                    <th>Report ID</th>
                    <th>Client Name</th>
                    <th>Name</th>
                    <th>Deviсe ID</th>
                    <th>Error Description</th>
                </tr>
                </thead>
                <tbody>
                <tr>
                    <td class="align-middle">{{ report.id }}</td>
                    <td class="align-middle">{{ report.client['name'] }}</td>
                    <td class="align-middle">{{ report.name }}</td>
                    <td class="align-middle">{{ report.deviceID }}</td>
                    <td class="align-middle">{{ report.description }}</td>
                </tr>
                </tbody>
            </table>
        </div>
        <div class="row my-3">
            <a class="btn btn-primary mx-auto" href="/report/new">Go Back</a>
        </div>
    </div>
{% endblock %}